title: Call assignment targets
pending:
    h: In queue ({count}/{max})
blocked:
    h: Withdrawn from queue ({count}/{max})
    reasons:
        allocated: Assigned to caller
        call_back_after: Call back later
        cooldown: Cooldown
        no_number: No number
        organizer_action_needed: Awaiting organizer
fulfilled:
    h: Goal fulfilled ({count}/{max})
