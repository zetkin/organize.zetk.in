import types from '../actions';

export default function actions(state = null, action) {
    console.log(action);
    /*
    switch (action.type) {
        case types.RETRIEVE_ACTIONS:
            
    }
    */
}
